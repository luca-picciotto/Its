const typeOfcards = ["character", "spell", "enemie"];
const typeOfMethods = ["attack", "buff", "damage", "duel"];
    let deck = [];
let card = { 
    type,
    properties: {
        strength,
        defense,
        health,
    },     
    method
}
function createCard(deck, i){
    deck[i].
}
